import React from "react";

function Search({ search }) {
    return (
        <form>
            <input type="text">

            </input>
            <button onClick={search}></button>
        </form>
    )
}

export default Search;